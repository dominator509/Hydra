# Test Case: <name>
Suite: unit|integration|conformance|e2e|smoke | Crate: <crate> | Spec: SPEC-XXX B<n>

## Behavior under test
<One sentence, observable.>
## Preconditions / fixtures
## Steps
## Expected result
<Exact output/assertion.>
## Failure notes
<What a failure most likely indicates; narrower diagnostic to run.>
Naming: `prop_*` property, `regress_<issue>` regression, `failure_*` failure-mode, `e2e_*` end-to-end, `tk_*` tokenkiller, `perf_*` performance.
